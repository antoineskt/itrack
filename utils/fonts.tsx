import { Inter } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  weight: ['900'],
  style: ['normal'],
});

export const interSmall = Inter({
  subsets: ['latin'],
  weight: ['500'],
});
