import { Inter } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  weight: ['700'],
  style: ['normal'],
});

export const interSmall = Inter({
  subsets: ['latin'],
  weight: ['500'],
});
